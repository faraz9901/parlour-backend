export enum Role {
    ADMIN = "admin",
    USER = "user",
    SUPER_ADMIN = "super_admin",
}

export enum TaskStatus {
    PENDING = "pending",
    IN_PROGRESS = "in_progress",
    COMPLETED = "completed",
}



